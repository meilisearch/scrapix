{
  // extend your base config so you don't have to redefine your compilerOptions
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "allowJs": false,
    "module": "esnext",
  },
  "include": [
    "src/**/*.ts",
    ".eslintrc.cjs"
  ]
}
